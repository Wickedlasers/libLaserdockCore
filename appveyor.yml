build: false

platform: x64

build_script:
  - mkdir build && cd build
  - cmake .. -G "%CMAKE_GENERATOR%" -A Win32 -DQTDIR="C:\Qt\5.13.2\msvc2017"
  - cmake --build . --config Release

environment:
  matrix:
    - APPVEYOR_BUILD_WORKER_IMAGE: Visual Studio 2017
      CMAKE_GENERATOR: Visual Studio 15 2017
